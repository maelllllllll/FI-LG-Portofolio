---
import Base from '../layouts/Base.astro';
import { sections, identite } from '../data/site.js';

// Une page par entrée de `sections` dans src/data/site.js.
export function getStaticPaths() {
  return sections.map((section, index) => ({
    params: { slug: section.slug },
    props: {
      section,
      suivante: sections[(index + 1) % sections.length],
    },
  }));
}

const { section, suivante } = Astro.props;
const nomComplet = `${identite.prenom} ${identite.nom}`;
---

<Base
  titre={`${section.titre} — ${nomComplet}`}
  description={section.chapo}
>
  <header class="tete">
    <div class="contenu">
      <p class="surtitre">{section.numero} — {section.sousTitre}</p>
      <h1 class="tete__titre">{section.titre}</h1>
      {section.chapo && <p class="tete__chapo">{section.chapo}</p>}
    </div>
  </header>

  {
    section.images.length > 0 ? (
      <ul class="galerie contenu">
        {section.images.map((image) => (
          <li class={`galerie__item apparait galerie__item--${image.format ?? 'portrait'}`}>
            <figure>
              <img src={image.src} alt={image.alt ?? ''} loading="lazy" />
              {image.legende && <figcaption>{image.legende}</figcaption>}
            </figure>
          </li>
        ))}
      </ul>
    ) : (
      <p class="contenu galerie__vide">
        Les visuels de cette section seront ajoutés prochainement.
      </p>
    )
  }

  <nav class="suite contenu" aria-label="Section suivante">
    <hr class="filet" />
    <a class="suite__lien" href={`/${suivante.slug}`}>
      <span class="surtitre">Chapitre suivant</span>
      <span class="suite__titre">{suivante.titre}</span>
    </a>
  </nav>
</Base>

<style>
  .tete {
    padding-top: clamp(7rem, 18vh, 12rem);
    padding-bottom: clamp(2.5rem, 7vw, 5rem);
  }

  .tete__titre {
    font-size: clamp(2.25rem, 9vw, 7rem);
    margin-top: 0.85rem;
  }

  .tete__chapo {
    max-width: 52ch;
    margin: clamp(1.25rem, 3vw, 2rem) 0 0;
    color: var(--texte-sourd);
    font-size: clamp(1rem, 0.95rem + 0.3vw, 1.2rem);
  }

  /* --- Galerie ------------------------------------------------------------ */

  .galerie {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(min(20rem, 100%), 1fr));
    gap: clamp(1rem, 3vw, 2.5rem);
    margin: 0 auto;
    padding-bottom: clamp(3rem, 8vw, 6rem);
    list-style: none;
  }

  /* Une image paysage occupe deux colonnes quand la place le permet. */
  .galerie__item--paysage {
    grid-column: span 2;
  }

  @media (max-width: 52rem) {
    .galerie__item--paysage {
      grid-column: span 1;
    }
  }

  .galerie figure {
    margin: 0;
  }

  .galerie img {
    width: 100%;
    object-fit: cover;
    background: var(--noir-doux);
  }

  .galerie__item--portrait img {
    aspect-ratio: 4 / 5;
  }

  .galerie__item--paysage img {
    aspect-ratio: 16 / 10;
  }

  .galerie__item--libre img {
    aspect-ratio: auto;
  }

  .galerie figcaption {
    margin-top: 0.8rem;
    font-size: 0.75rem;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: rgb(242 236 225 / 0.45);
  }

  .galerie__vide {
    color: var(--texte-sourd);
    padding-bottom: clamp(3rem, 8vw, 6rem);
  }

  /* --- Chapitre suivant --------------------------------------------------- */

  .suite__lien {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    padding-block: clamp(2rem, 5vw, 3.5rem);
  }

  .suite__titre {
    font-family: var(--display);
    font-size: clamp(1.75rem, 6vw, 4rem);
    text-transform: uppercase;
    line-height: 1;
    transition: color var(--moyen);
  }

  .suite__lien:hover .suite__titre {
    color: var(--accent);
  }
</style>
